# The Human Latency

The public site for The Human Latency — hidden systems, human consequences.

Plain HTML and one stylesheet. No framework, no build step, no tracking scripts.
Served by GitHub Pages.

Videos are self-hosted in `media/` rather than embedded from YouTube, because the
brand promises local-first and no tracking, and a YouTube embed would break both.
